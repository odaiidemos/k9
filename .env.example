# K9 Operations Management System - Environment Configuration
# Copy this file to .env and modify the values for your deployment

# Database Configuration
POSTGRES_DB=k9operations
POSTGRES_USER=k9user
POSTGRES_PASSWORD=secure_password_change_me

# Application Configuration
# CRITICAL: Generate a strong random string for production!
# You can use: python -c "import secrets; print(secrets.token_urlsafe(32))"
SESSION_SECRET=generate_a_secure_random_string_for_production

# Database URL (automatically constructed from above variables)
# DATABASE_URL=postgresql://k9user:secure_password_change_me@db:5432/k9operations

# Web Server Configuration
WEB_PORT=80
GUNICORN_WORKERS=4

# SECURITY NOTES:
# 1. Change POSTGRES_PASSWORD to a strong, unique password
# 2. Generate a secure SESSION_SECRET using cryptographically secure methods
# 3. The default admin account (admin/admin123) is created automatically
#    IMPORTANT: Change or disable this account immediately after first login!
# 4. For production deployment:
#    - Use a firewall to restrict database access
#    - Enable SSL/TLS encryption
#    - Set up regular database backups
#    - Monitor logs for security issues
# 5. Consider using Docker secrets or a key management service for sensitive values

# Production Deployment Checklist:
# [ ] Changed POSTGRES_PASSWORD from default
# [ ] Generated secure SESSION_SECRET
# [ ] Changed default admin credentials (admin/admin123)
# [ ] Configured reverse proxy with SSL
# [ ] Set up database backups
# [ ] Configured log monitoring
# [ ] Restricted network access to database